# Создаем пустой словарь
my_dict = {}
# Заполняем словарь с помощью цикла
for num in range(10, -6, -1):  # от 10 до -5 включительно
    my_dict[num] = num ** num  # возводим число в степень, равную этому числу
# Выводим получившийся словарь
print(my_dict)
