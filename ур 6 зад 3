# Вводим целые числа A и B
A, B = map(int, input("Введите два целых числа A и B (A ≤ B): ").split())
# Создаем список для хранения четных чисел
even_numbers = []
# Перебираем все числа от A до B
for num in range(A, B + 1):
    if num % 2 == 0:  # Проверяем, является ли число четным
        even_numbers.append(num)
# Выводим четные числа через пробел
print("Четные числа на отрезке:", ' '.join(map(str, even_numbers)))
