import math

# Вводим натуральное число X
X = int(input("Введите натуральное число X (X ≤ 2e9): "))
# Инициализируем счетчик делителей
divisor_count = 0
# Перебираем все числа от 1 до √X
for i in range(1, int(math.sqrt(X)) + 1):
    if X % i == 0:  # Если i является делителем
        divisor_count += 1  # Увеличиваем счетчик для делителя i
        if i != X // i:  # Если i не равен X/i, увеличиваем счетчик еще раз
            divisor_count += 1
# Выводим количество делителей
print("Количество натуральных делителей числа X:", divisor_count)
