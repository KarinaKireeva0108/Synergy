# Вводим количество чисел N
N = int(input("Введите количество целых чисел (N): "))
# Инициализируем счетчик нулей
zero_count = 0
# Запрашиваем N целых чисел и подсчитываем количество нулей
for i in range(N):
    number = int(input(f"Введите целое число {i + 1}: "))
    if number == 0:
        zero_count += 1
# Выводим количество нулей
print("Количество нулей:", zero_count)
